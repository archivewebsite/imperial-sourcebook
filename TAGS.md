# TAGS

This file is the canonical tag registry for Imperial Sourcebook. Tags should improve retrieval without duplicating the folder map.

## Registered Roots

- `subject/`: topic or domain tags, such as `subject/math`, `subject/language`, or `subject/design`.
- `type/`: note-form tags, such as `type/concept`, `type/clipping`, `type/glossary`, `type/person`, `type/moc`, `type/soal`, `type/worked-example`, `type/stub`, or `type/cheatsheet`.
- `status/`: lifecycle tags, such as `status/stub`, `status/draft`, `status/review`, or `status/evergreen`.
- `level/`: learning-depth tags, such as `level/beginner`, `level/intermediate`, or `level/advanced`.

## Rules

- Add a tag here before using it as a recurring convention.
- Keep tags lowercase and use hyphens for multiword names.
- Prefer one useful tag over several decorative tags.
- Do not add tags to old notes retroactively unless a dedicated tagging pass asks for it.
- Do not use tags to mirror every folder name; folder placement remains the primary structure.

## Registered Tags

### Subject

- `subject/ai`
- `subject/design`
- `subject/language`
- `subject/learning`
- `subject/literacy`
- `subject/math`
- `subject/psychology`

### Type

- `type/concept`
- `type/clipping`
- `type/cheatsheet`
- `type/glossary`
- `type/moc`
- `type/person`
- `type/soal`
- `type/stub`
- `type/worked-example`

### Status

- `status/stub`
- `status/draft`
- `status/review`
- `status/evergreen`

### Level

- `level/beginner`
- `level/intermediate`
- `level/advanced`

## Naming Rules

- Use lowercase tags.
- Use hyphens for multiword tag segments.
- Use slash roots only when the root is registered above.
- Prefer a folder, MOC, or wikilink when the label only repeats where the note already lives.

## Legacy Drift

Existing notes contain mixed-case, imported, color-code, numeric, and heading-derived tags from earlier captures. Treat those as legacy noise unless a future cleanup pass explicitly normalizes them into the registered taxonomy.
